resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"

defaultBuckets: "loki-data,prometheus-data"
rootUser: "admin"
rootPassword: "admin"

persistence:
  enabled: false 

minio:
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
