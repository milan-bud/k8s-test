minio:
  defaultBuckets: "loki-data,prometheus-data"
  rootUser: "admin"
  rootPassword: "admin12345"

  persistence:
    enabled: true
    storageClass: "standard"
    size: 5Gi

  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
