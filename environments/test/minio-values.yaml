defaultBuckets: "loki-data,prometheus-data"
rootUser: "admin"
rootPassword: "admin"

persistence:
  enabled: true
  storageClass: "standard"
  size: 5Gi

# Bitnami MinIO chart primary block
primary:
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"

# Standalone chart block fallback
resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
