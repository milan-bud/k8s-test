minio:
  defaultBuckets: "loki-data,prometheus-data"
  rootUser: "admin"
  rootPassword: "admin"
  persistence:
    enabled: true
    size: 10Gi
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
