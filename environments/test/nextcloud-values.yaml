nextcloud:
  nextcloud:
    host: "nextcloud.local"
    password: "Admin12345"

  configs:
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'trusted_domains' => 
        array (
          0 => 'localhost',
          1 => 'nextcloud.local',
          2 => '*',
        ),
      );

  persistence:
    enabled: true
    size: 8Gi

  postgresql:
    enabled: false
